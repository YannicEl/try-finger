{
	"name": "test",
	"version": "1.0.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"watch": "vitest",
		"test:dev": "firebase emulators:exec --only firestore --project dev \"vitest run\"",
		"test:prod": "firebase emulators:exec --only firestore --project prod \"vitest run\"",
		"serve:dev": "firebase emulators:start --only firestore --project dev",
		"serve:prod": "firebase emulators:start --only firestore --project prod"
	},
	"devDependencies": {
		"dotenv": "^16.0.1",
		"firebase-tools": "11.0.1",
		"vitest": "^0.13.1"
	},
	"dependencies": {
		"@firebase/rules-unit-testing": "^2.0.2",
		"firebase": "^9.8.2"
	},
	"pnpm": {
		"peerDependencyRules": {
			"ignoreMissing": [
				"@firebase/app-compat",
				"@firebase/app-types"
			]
		}
	}
}
